联信装机(aixiwu.cn)技术运营，自己管腾讯云服务器。中文直接沟通，执行力强不做冗长讨论。视觉标准高——发现 UI 问题会直接给修复方案，不依赖我猜审美。站点已部署 Hermes Web UI (v0.5.17)，代理到内部 6060。

本次会话：提交了详细安全审计清单（10+ 项 P0/P1），要求按顺序逐个修复。修完后要求全站检测，又发现 BUG-1（XSS）、BUG-2（null user 落库）。

**用户技术风格**：
- 精确：certbot `--expand` vs 仅 `-d` 的区别记得比我还清楚；正则 `[^#*]` 的 bracket expression 行为当场指出我的错误
- 直接：不绕弯，给结论+证据，不做冗长讨论
- 纠错优先：发现我的推理有漏洞会直接指出，不等我自悟
- 发版闭环意识极强：git 仓库必须等于线上逻辑，任何未入库的代码都是断点，会追到底
§
本次会话：安全审计清单（10+ P0/P1）逐个修复；全站检测发现 BUG-1（XSS）、BUG-2（null user 落库）；定位到 HERMES_HOME 环境变量在 bash -lic 启动时被 profile 重置，导致 avatar 进程读错 .env（读根目录而非各自目录），解决方案：根 .env 补 DEEPSEEK_API_KEY + FEISHU_ALLOW_ALL_USERS=true。

**通讯偏好**：极简直接，问什么答什么。"小迪？"触发时期望确认回复而非诊断报告。

**铁律**：用户说「小迪」= 主 bot Hermes小迪（open_id: 7638611309260524739），不是用户自己。发消息时必须 @ Hermes小迪，不能 @ 其他名称。
§
**aixiwu.cn/myai 装饰风格要求**（2026-05-15 确认）：
- 芯片意象/科技风装饰不得作为主视觉，不得比内容抢眼
- 禁止：粗描边 + 三面引脚/凸起、芯片外框（内外双层矩形+三面凸起）
- 允许：极淡背景渐变（opacity ≤0.08）、细点阵（≤2px 线宽），均需 pointer-events:none
- 验收标准：第一眼应看到对话内容，不是框线
- 反复要求删除 chip-frame、ca-edge 等粗线元素，立场强硬不退让
§
联信装机(aixiwu.cn)技术运营。中文直接沟通，执行力强不做冗长讨论，视觉标准高。

**本次会话关键行为**：
- 自己打开 F12 console 定位到 SyntaxError（msgInput 重复声明），不依赖我描述
- 发现我反复试探/走弯路会直接说「你在干什么」
- 对 AI 输出的代码质量要求严格——语法错误会导致整页 JS 失效，会追到底

**技术风格**：精确、直接、纠错优先、发版闭环意识极强。不写测试用例，要求我执行测试并给出验证结果。